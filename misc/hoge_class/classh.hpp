class Ship
{
    public:
        double length;
        double width;
        double weight;
        Ship(double length, double width, double weight);

        void hello();
        void say_param();
    
    private:
        void thnak();
};